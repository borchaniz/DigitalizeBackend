
<html>
<head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" href="{{ asset('css/index.css') }}">
    <style>
        body{
            background-size: 100%;
            background-image: url('img/formBG.jpg');
            margin-top: 60px;
            background-repeat: no-repeat;
        }
        .col-xs-6, .col-xs-12{
            height:100%;
            padding-top: 15%;
            font-size:60px;
            text-align: center;
            color:white;
            -webkit-box-shadow: 1px 2px 10px -2px rgba(0,0,0,0.75);
            -moz-box-shadow: 1px 2px 10px -2px rgba(0,0,0,0.75);
            box-shadow: 1px 2px 10px -2px rgba(0,0,0,0.75);
        }
        .col-xs-6:hover{
            background-color: rgba(0,0,0,0.3);
        }

        @media (max-width: 768px) {
            form{
                width:70%
            }
        }
        
    </style>
</head>

<body>
    <div class="navbar navbar-inverse navbar-fixed-top opaque-navbar" style="background-color: black;: ">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand" href="#"><img src="img/digitalize international1.png" height="200%" style="margin-top:0px; position:relative; top:-30%"></a>
            </div>
            {% if msg=='connected' %}
                <ul class="nav navbar-nav navbar-right">
                    <li><a href="{{ path('logout') }}">Quitter l'interface Administrateur <span class="glyphicon glyphicon-log-out"></span></a></li>
                </ul>
            {% endif %}
        </div>
    </div>
    {% if msg=='connected' %}<div class="row">
        <a href="{{ path('RecruitList') }}">
            <div class="col-xs-6">Recrutement</div>
        </a>
        <a href="{{ path('RDVList') }}">
            <div class="col-xs-6">Rendez-vous</div>
        </a>
    {% else %}
        <div class="col-xs-12">Vous n'êtes pas connctés au compte administrateur</div>
    {% endif %}
    </div>
</body>
</html>